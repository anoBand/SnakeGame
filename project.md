# Project Optimization Analysis: Snake Game (Assembly)

이 문서는 `game.asm` 소스 코드의 성능 문제점을 분석하고 이를 해결하기 위한 최적화 방안을 정리합니다.

## 1. 현재 문제점 분석

### 1.1. 과도한 `printf` 호출 (Rendering Bottleneck)
*   **문제:** `render_game` 함수에서 매 픽셀(문자)마다, 그리고 매 색상 변경마다 `printf`를 호출합니다.
*   **영향:** $40 \times 20$ 그리드에서 최소 800번 이상의 `printf`가 발생하며, 색상 코드까지 포함하면 프레임당 수천 번의 라이브러리 함수 호출 및 시스템 호출 오버헤드가 발생합니다. 이는 화면 깜빡임과 CPU 점유율 상승의 주원인입니다.

### 1.2. 비효율적인 충돌 및 렌더링 검사 ($O(N^2)$ 복잡도)
*   **문제:** `render_game` 루프 내에서 각 좌표(X, Y)에 무엇이 있는지 확인하기 위해 모든 사과(`corpse_apples`)와 뱀의 몸통을 전수 조사(Linear Search)합니다.
*   **영향:** `corpse_apples`가 최대 800개까지 늘어날 수 있는데, 800개의 타일을 그릴 때마다 800개의 배열을 뒤지면 프레임당 $800 \times 800 = 640,000$번의 비교 연산이 수행됩니다.

### 1.3. 데이터 이동 오버헤드 (Snake Movement)
*   **문제:** 뱀이 한 칸 이동할 때마다 몸통 배열의 모든 요소를 한 칸씩 뒤로 미는(Shifting) 방식을 사용합니다.
*   **영향:** 뱀의 길이가 길어질수록 메모리 복사 연산이 증가합니다 ($O(Length)$).

### 1.4. 불필요한 ANSI 색상 초기화
*   **문제:** 매 문자 출력 후 `ansi_reset`을 호출하여 색상을 초기화합니다.
*   **영향:** 다음 문자가 같은 색상이거나 기본 색상인 경우에도 불필요한 이스케이프 시퀀스를 출력하여 터미널 처리 부하를 가중시킵니다.

---

## 2. 해결 방안 (Optimization Strategies)

### 2.1. 프레임 버퍼링 (Double Buffering Concept)
*   **방안:** 메모리 내에 $40 \times 20$ 크기의 캐릭터 버퍼와 색상 버퍼를 생성합니다.
*   **효과:** 한 프레임의 모든 내용을 메모리에서 먼저 구성한 뒤, 단 한 번의 `printf`나 `WriteConsole` 호출로 전체 화면을 출력합니다. 시스템 호출 횟수를 획기적으로 줄일 수 있습니다.

### 2.2. 그리드 맵(2D Array) 기반 조회
*   **방안:** 현재 상태를 나타내는 2차원 배열 `game_map[20][40]`을 유지합니다.
*   **효과:** 렌더링 시 루프를 돌며 검색할 필요 없이 `game_map[y][x]`의 값을 바로 참조하면 됩니다. 시간 복잡도가 $O(N^2)$에서 $O(1)$(픽셀당)로 감소합니다.

### 2.3. 순환 버퍼(Circular Buffer) 활용
*   **방안:** 뱀의 몸통 좌표를 관리할 때 배열 시프팅 대신 순환 버퍼를 사용합니다.
*   **효과:** 머리(Head)와 꼬리(Tail) 인덱스만 갱신하면 되므로 뱀의 길이와 상관없이 이동 연산이 $O(1)$이 됩니다.

### 2.4. 상태 기반 색상 변경 (State-based Color Update)
*   **방안:** 현재 출력 중인 색상 상태를 변수에 저장하고, 다음 문자의 색상이 다를 때만 ANSI 코드를 출력합니다.
*   **효과:** 불필요한 문자열 전송을 줄여 터미널 렌더링 속도를 향상시킵니다.

### 2.5. `GetAsyncKeyState` 최적화
*   **방안:** 매 프레임 모든 키를 검사하는 대신, 필요한 입력만 그룹화하거나 메시지 큐 방식을 고려할 수 있습니다. (현재 규모에서는 큰 영향은 없으나 확장성 면에서 유리)

## 3. 우선순위 제안
1.  **그리드 맵 도입:** 렌더링 및 충돌 알고리즘 개선 (가장 큰 성능 향상)
2.  **프레임 버퍼링:** 화면 깜빡임 제거 및 입출력 효율화
3.  **순환 버퍼:** 데이터 처리 로직 최적화
