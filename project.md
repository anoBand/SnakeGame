작성해주신 초안을 바탕으로, 64비트 어셈블리 환경에서 실제로 구현할 때 발생할 수 있는 기술적 문제(동시 입력, 화면 깜빡임, 메모리 관리)를 해결할 수 있도록 내용을 대폭 보강하고 구체화한 **완성형 프로젝트 기획서**를 작성해 드립니다.

어셈블리로 2인용 실시간 게임을 만드는 것은 난이도가 높으므로, 구조를 탄탄하게 잡는 것이 중요합니다.

---

# 🐍 64-bit Assembly 2-Player Snake Game Project

## 1. 기술 스택 및 개발 환경

* **언어:** x86-64 Assembly (Windows 64-bit 타겟)
* **어셈블러:** NASM (`nasm -f win64`)
* **링커:** GCC (MinGW-w64)
* **파일 구성:** 단일 `game.asm` 파일 구성 (유지보수를 위해 기능별로 구역을 명확히 나누고 **모든 주요 로직에 상세 주석 필수**)
* **외부 라이브러리 (C 표준 라이브러리 및 Windows API 활용):**
* `printf` (출력 및 ANSI 색상 제어)
* `Sleep` (Windows API - 프레임 속도 조절)
* `GetAsyncKeyState` (Windows API - 2인 동시 키보드 입력 처리)
* `rand`, `srand`, `time` (랜덤 사과 생성)



## 2. 게임 핵심 메커니즘

### 2.1. 플레이어 조작 및 이동

* **Player A:** `W`, `A`, `S`, `D` (상하좌우)
* **Player B:** `↑`, `↓`, `←`, `→` (화살표 키)
* **이동 규칙:**
* 두 뱀은 동일한 속력(프레임 레이트)으로 이동합니다.
* 프레임당 입력이 없어도 마지막으로 입력된 방향으로 계속 전진합니다.
* 자신의 현재 진행 방향과 정반대 방향으로는 즉시 이동할 수 없습니다. (예: 오른쪽으로 가던 중 왼쪽 입력 무시)



### 2.2. 충돌 판정 및 데스(Death) 로직

* **벽 충돌:** 맵의 테두리(터미널 창 끝)에 닿으면 사망.
* **자살 (Self-Collision):** 자신의 진행 방향에 자신의 몸통이 있으면 사망.
* **상대방 충돌 (PvP Collision):** * A의 머리가 B의 몸통에 부딪히면 A가 사망.
* **헤드 투 헤드(Head-to-Head):** 머리끼리 부딪힐 경우, 길이가 짧은 뱀이 사망 (길이가 같으면 무승부로 동시 사망).


* **사망 페널티 (시체 파밍 로직):**
* 사망한 뱀은 즉시 소멸하며, 해당 뱀의 머리와 몸통이 있던 **모든 좌표가 '사과'로 변환**됩니다. (생존한 플레이어에게 역전/스노우볼 기회 제공)



### 2.3. 성장 및 점수 (사과 시스템)

* **사과 생성:** 맵 위에 무작위 좌표에 주기적으로 사과가 1개씩 스폰됩니다. (단, 뱀의 몸통이 없는 빈 공간에만 스폰)
* **길이 증가 방식 (어셈블리 배열 처리):**
* 머리가 사과가 있는 좌표로 이동할 때, 꼬리 배열을 삭제(Pop)하지 않고 그대로 둡니다.
* 기존 머리가 있던 좌표는 몸통으로 취급되고, 새로운 머리 좌표가 배열의 맨 앞에 추가(Push)됩니다.


* **점수:** 사과 1개 획득 시 10점 획득.

## 3. UI 및 시각적 요소 (디자인 가이드)

64비트 터미널 환경에서는 **ANSI Escape Sequence**를 적극 활용하여 화면 깜빡임 없이 색상과 위치를 제어합니다.

### 3.1. 화면 레이아웃 (ANSI 커서 이동 활용)

* 터미널 창을 지울 때마다 `system("cls")`를 사용하면 화면이 심하게 깜빡입니다. 대신 `\033[H` (커서를 0,0으로 이동)를 사용하여 화면을 덮어쓰는 방식으로 렌더링해야 부드럽습니다.
* **좌측 상단:** `[Player A Score: 000]` (파란색 텍스트)
* **우측 상단:** `[Player B Score: 000]` (빨간색 텍스트)

### 3.2. 아스키 아트 및 색상 부여

C 라이브러리의 `printf`에 ANSI 코드를 섞어 출력합니다. (예: `\x1b[31m` = 빨간색)

| 요소 | 추천 기호 (ASCII/Extended) | ANSI 색상 | 설명 |
| --- | --- | --- | --- |
| **Player A (머리)** | `▣` 또는 `O` | **Cyan** (`\x1b[36m`) | 이동 방향에 따라 `▲, ▼, ◀, ▶` 로 디테일 추가 가능 |
| **Player A (몸통)** | `■` 또는 `o` | **Blue** (`\x1b[34m`) |  |
| **Player B (머리)** | `▣` 또는 `O` | **Magenta** (`\x1b[35m`) |  |
| **Player B (몸통)** | `■` 또는 `o` | **Red** (`\x1b[31m`) |  |
| **사과** | `★` 또는 `♥` | **Yellow** (`\x1b[33m`) | 가시성이 좋은 기호 선택 |
| **배경/빈 공간** | ` ` (공백) | 없음 (`\x1b[0m`) |  |
| **벽/테두리** | `▒` 또는 `#` | **White** (`\x1b[37m`) | 맵 테두리 제한 |

## 4. 어셈블리 구현 주요 자료구조 (Data Section)

```assembly
section .data
    ; 화면 크기
    MAX_X equ 80
    MAX_Y equ 24
    
    ; 플레이어 데이터 (최대 길이 100 가정)
    playerA_X resb 100  ; A의 X좌표 배열
    playerA_Y resb 100  ; A의 Y좌표 배열
    playerA_Len dq 3    ; A 현재 길이
    playerA_Dir db 'R'  ; A 현재 방향 (U, D, L, R)
    playerA_Score dq 0
    
    ; B 데이터도 A와 동일한 구조로 선언...

```

## 5. 실행 흐름 (Game Loop)

어셈블리 코드의 메인 루프(`game_loop:`)는 다음 순서로 끝없이 반복됩니다.

1. **Input (입력 처리):** `GetAsyncKeyState` 함수를 두 번 호출하여 A(WASD)와 B(방향키)의 입력을 **동시에 독립적으로** 감지하여 방향 변수를 갱신합니다.
2. **Update (물리 연산):** * 방향 변수에 따라 각 뱀의 새로운 머리 좌표를 계산합니다.
* 사과를 먹었는지, 벽에 부딪혔는지, 서로 부딪혔는지 판정합니다.


3. **Array Shift (메모리 이동):** * 몸통 좌표 배열을 뒤로 한 칸씩 밀어내고, 0번 인덱스에 새로운 머리 좌표를 넣습니다.
4. **Render (화면 출력):** * 화면 커서를 0,0으로 옮긴 뒤, 2중 루프를 돌며 현재 맵 상태(배열 데이터)에 맞춰 텍스트와 색상을 `printf`로 출력합니다.
5. **Delay (프레임 고정):** `Sleep(50)` (50ms 대기)을 호출하여 게임 속도를 일정하게 맞춥니다.

## 6. 빌드 및 실행 명령어

작성하신 명령어에서 오타를 수정한 최종 버전입니다. Windows 환경의 MSYS2/MinGW 터미널에서 실행합니다.

```bash
# 1. 64비트 오브젝트 파일로 어셈블
nasm -f win64 game.asm -o game.obj

# 2. GCC를 이용해 C 라이브러리 및 Windows API와 링킹
gcc game.obj -o game.exe

# 3. 실행 (PowerShell/CMD 기준)
.\game.exe

```

---

### 💡 64비트 전환을 위한 추가 팁

기존 16비트 스네이크는 `_getch`나 `int 16h`를 사용했지만, 2인용 게임에서는 A가 누르는 동안 B의 입력이 씹히는(Blocking) 문제가 발생합니다. 따라서 `extern GetAsyncKeyState`를 상단에 선언하시고, 특정 키보드 키(VK_UP, 'W' 등)가 현재 눌려있는지 실시간으로 스캔하는 윈도우 API를 사용하는 것이 이 프로젝트 성공의 핵심 키가 될 것입니다.